import { StyleSheet } from "react-native";

export const stylehome = StyleSheet.create({

    container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
    },

    title: {
        fontSize: 17
    }
})